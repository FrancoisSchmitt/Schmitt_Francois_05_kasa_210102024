import './index.css';
function Tag(tags) {
      return (
            <div className="lol">
                  <p className="tags">{tags.tagName}</p>
            </div>
      );
}

export default Tag;
